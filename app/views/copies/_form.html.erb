<%= form_for (@copy) do |f| %>

<div class="field">
  <%= f.label :copy_number %>
  <%= f.text_field :copy_number %>
</div>

<div class="field">
  <%= f.label :book_id %>
  <%= f.text_field :book_id %>
</div>

<div class="actions">
  <%= f.submit %>
</div>
<% end %>